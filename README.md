# GitHub workflows

My repositories for commonly-used github workflows.

- `SSH_PRIVATE_KEY`: private key in your local machine (corresponding to the public key in the git platform)
- `CODECOV_TOKEN`: token generated in Codecov for repository
- `API_KEY`: api for zhipu (or other LLM models)
